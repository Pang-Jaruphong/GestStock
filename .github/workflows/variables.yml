name: variable.yml

on:
  workflow_dispatch:

jobs:
  mon-job:
    runs-on: ubuntu-latest

    env:
      PRENOM: "Jaruphong"

    steps:
      - name: Salutation personnalisée
        run: echo "Bonjour $PRENOM, bienvenue dans le module ICT324"